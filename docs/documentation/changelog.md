# Documentation changelog

**Status:** Active  
**Owner:** Documentation owner  
**Reference when:** Reviewing material changes to the repository operating
system.  
**Agent obligation:** Add an entry for changed authority, workflow, architecture
governance, or product interpretation; omit typo-only edits.

## 2026-08-13

- Accepted PD-020 and implemented a visible multi-song library with one honest
  playable choice, three unavailable rights-pending candidates, and no camera
  or audio side effect before `Play Mo Li Hua`.
- Replaced the explanatory scored-play rails with a full-stage, elderly-first
  composition: compact player location, one 56 px-or-larger current action,
  112 px-or-larger cue icons, four progress dots, short time, and 64 px-or-
  larger Pause/Resume/Stop controls.
- Added the local muted project-authored `DB-MV-001` Mo Li Hua motion visual,
  poster fallback, playback lifecycle tests, exact provenance/checksums, and
  the explicit rule that reduced dynamics never freezes or replaces the MV.
- Added production-browser containment, axe, standing/seated, tracking-loss,
  pause/resume, 200% library, and screenshot evidence while retaining owner
  asset approval, older-adult comprehension, real-camera/device, and manual
  assistive-technology checks as open release gates.

## 2026-08-12

- Accepted PD-019 and implemented the elderly-first `Now / You / Next`
  gameplay hierarchy with separate framing/home language, a visible
  player-state rail, four timing stages, and four session phases.
- Established browser-enforced viewing-distance floors for live state and
  Pause/Resume, moved narration and volume controls into the paused comfort
  surface, and kept tracking uncertainty explicitly unscored.
- Added standing, seated, reduced-dynamics, pause, tracking-recovery,
  accessibility, and 1280 x 720 / 1024 x 720 containment evidence while
  retaining real-camera and older-adult usability as open gates.

## 2026-07-28

- Removed the permanently reserved empty column from setup screens, kept
  calibration split across common laptop widths, and moved the gameplay HUD
  into a contained overlay so the playfield remains full-width and centred.
- Reframed movement rehearsal around a camera-first practice stage. Move
  progress, active cue, centre/home guidance, and tracking status now sit
  beside or over the preview; a drawn home marker and neutral-return latch make
  the centred default position part of the interaction rather than extra copy.
- Made the primary laptop setup journey responsive to viewport height as well
  as width. Header height, display type, spacing, controls, artwork, setup
  lists, and practice content now compress within accessibility floors; a
  production-browser regression guards against page-level scrolling at
  1280 x 720.
- Kept long progress, privacy, stored-record, and zoomed reading surfaces on
  normal document scrolling instead of introducing cramped nested scroll
  regions.

## 2026-07-26

- Established `docs/` as the human/AI operating system.
- Added authority order and task-based routing.
- Captured the edited PRD decisions: 舞伴 name, adults 60-75, laptop-first demo,
  standing-first plus seated hand/finger route, `茉莉花` music direction, and
  consented WeChat test check-in.
- Added local-first, single-primary-player, wellness-claim, and caregiver
  check-in ADRs.
- Added AI, engineering, frontend, product, context, workflow, governance,
  templates, documentation, enforcement, and integration guidance.
- Added local documentation validation.
- Verified the 85-file documentation inventory, two repository entrypoints,
  required task routes, H1 structure, and internal links with Node v25.9.0.
- Opened time-boxed M1 dependency evaluations for the browser application,
  on-device landmarks, and deterministic test tooling before any manifest
  change.
- Accepted ADR-0005 after the local camera, pose, hand, audio, package-audit,
  and model-licence spike; resolved stack gates KI-001 and KI-007.
- Recorded the warm editorial UI direction and remaining human-trace and
  demo-device evidence separately from the successful headless proof.
- Implemented the Vite/React consent-to-result slice for standing and seated
  play with local-only storage, visible synthetic fallback, bilingual copy,
  reduced motion, and deterministic scoring.
- Added privacy/camera, countdown, session, classifier, scoring, quality, and
  IndexedDB tests; recorded the passing local build and browser journeys in
  `engineering/m2-vertical-slice-evidence.md`.
- Implemented the versioned personal median/MAD trend rule, deterministic
  simulated history, separate supporter grant/revocation, check-in preview,
  idempotent fail-closed notification service, and IndexedDB v2 migration.
- Recorded that the WeChat transport remains deliberately unavailable pending
  an owner-selected current test channel; no delivery is claimed.
- Replaced the technical beat draft with a project-authored procedural
  `茉莉花` arrangement, documented the public-domain score reference and
  checksum, and retained project-owner listening as an open release gate.
- Added the PRD-required unusual-day context choice so pain, illness, unusual
  fatigue, or camera trouble excludes trend input without discarding the game
  result or participation.
- Persisted the explicit reduced-motion choice and limited compact-screen
  blocking to movement phases so progress, privacy, and result surfaces remain
  readable at a 200% equivalent viewport.
- Added deterministic production-bundle precaching for the application,
  self-hosted models, and MediaPipe runtime; verified an offline reload and
  synthetic spectator start in Chrome.
- Added production Chrome coverage for axe scans, keyboard
  Pause/Resume/Stop, unusual-day exclusion, reduced-motion persistence,
  narrow-screen behavior, trend/sharing consent, and offline load.
- Added a least-privileged, SHA-pinned GitHub Actions pipeline that reuses the
  local verification contracts and installs Chrome through Playwright.
- Added enforced JavaScript, CSS, total-client, and required-offline-asset
  bundle budgets.
- Published private Sites version 2 and verified the signed-out ChatGPT access
  gate; owner-authenticated application smoke remains open.
- Added a requirement-by-requirement PRD acceptance audit that separates local,
  device, owner-account, research, and submission evidence.
- Aligned the playable loop to the PRD's unscored warm-up and 60/75/75-second
  phases; added stable calibration, two-repetition rehearsal, forgiving
  Nearly feedback, bounded adaptive preview support, companion quality gates,
  weekly participation, independent volume controls, and local debug metrics.
- Replaced direct synthetic observations with deterministic pose/hand
  landmark replay through the production classifiers and scoring path.
- Persisted interrupted active gameplay as explicitly invalid local summaries
  with typed reasons, removed empty-session phantom scoring/participation, and
  added a privacy inspector for exact locally stored JSON records.
- Added opt-in fixed-caption browser cue narration with bounded cue volume;
  narration is off by default, receives no player data, and is not required for
  offline or accessible play.
- Added the captured returning-player journey: the home screen reuses the last
  played mode and shows the approximate four-minute duration, while camera
  purpose/permission, safety, and shortened calibration remain mandatory.
  Simulated sessions do not activate this route or weekly participation.
- Resolved KI-012 by installing a clean archived checkout under the exact
  Node.js `24.18.0` and npm `11.16.0` contract, finding zero vulnerabilities,
  and passing the complete automated verification matrix.
- Replaced the gameplay loop's sub-budget 70 ms camera inference throttle with
  a tested 30 ms cadence contract. This removes the software-imposed failure
  without claiming the still-open real-camera/demo-device performance gate.
- Added a development-only, real-camera movement evidence report that
  aggregates expected-versus-detected cue counts, scoreable/match rates, and
  matched timing p95 without cue IDs, attempts, media, landmarks, or saved
  traces. KI-002 and KI-011 remain open until representative human/device runs.
- Replaced the stale proposed local-development placeholders with the exact
  pinned setup, command contract, synthetic route, and real-camera evidence
  collection runbook.
- Added a visibly simulated, classifier-backed tracking-loss regression that
  verifies guidance, recovery, quality-invalid persistence, and trend
  exclusion in production Chrome. Added separate captured-history coverage
  proving that participation credit remains independent from trend validity;
  real-camera recovery evidence remains an open device gate.
- Extended the production-browser permission journey to prove that a denied
  camera shows actionable guidance and can recover into the explicitly
  labelled camera-free demonstration without bypassing disclosure.
- Added component proof that a rejected on-device landmark-model load shows
  the existing recovery guidance and routes only through the explicit
  simulated fallback, without falsely completing calibration.
- Closed the local supporter revocation window by re-reading consent and
  recipient binding immediately before transport. Forced revocation and
  binding-substitution races now fail closed with no provider call or send
  audit; future server-side atomicity remains an external transport gate.
- Hardened the IndexedDB session read boundary with deep schema validation and
  exact-shape reconstruction. Malformed nested records are ignored while
  unexpected stored fields are removed before history, trends, or the local
  privacy inspector can consume them.
- Extended the development-only real-camera evidence artifact with aggregate
  active-session render/inference rates, inference duration median/p95,
  confidence, unclear-frame episodes/reasons, pauses, and audio-clock drift.
  The artifact remains ephemeral and excludes media, landmarks, cue IDs,
  attempts, and per-frame timings; human/device capture remains open.
- Activated the implemented project-structure contract and corrected the
  technical-debt register's stale pre-scaffold wording.
- Added a development-only local download for the already aggregate,
  privacy-safe camera evidence JSON so dated human/device records can use the
  exact artifact without copying rendered text.
- Resolved KI-003 with the implemented hand-provider spike, separate seated
  acceptance criteria, classifier/replay tests, and seated browser journey.
  Resolved KI-006 with accepted ADR-0004 and the implemented non-diagnostic
  check-in boundary.
- Rechecked the official Tencent event page on 26 July 2026. It still exposes
  only the overall 1 July-30 September event window, so KI-008 remains open
  pending the project-specific submission portal or organiser confirmation.
- Added task-heading focus on reading-surface transitions so screen-reader and
  keyboard users are oriented to home, disclosure, progress, sharing, and
  result before continuing through the task. Manual VoiceOver and
  forced-colour smoke remain open.
- Isolated production Chrome verification on dedicated port 4174 with preview
  reuse disabled after discovering that a long-running server from another
  worktree could otherwise satisfy Playwright's readiness check.
- Added explicit local-data loading, unavailable, and retry states. Failed
  reads no longer masquerade as empty history or sharing-off, failed database
  opens can be retried with a fresh connection, and rejected grant writes stay
  ungranted. Session completion continues to require a successful local
  summary save.
- Replaced the hidden audio-clock fallback with a pre-countdown recovery
  screen. Retry opens a fresh Web Audio context; the alternative silent
  practice remains visibly labelled, records `clock-error`, and cannot shape a
  personal trend. Pending preparation is cancelled on exit so late adapter
  completion cannot start a stale session.
- Converted fatal Web Audio pause/resume rejection during scored play into an
  immediate trend-invalid result. Scoring no longer continues against a
  corrupted clock, participation is preserved, and the player receives a
  specific non-blaming explanation.
- Persisted the chosen Simplified Chinese or English interface, synchronised
  the document language for assistive technology, and grouped language with a
  true reduced-dynamics switch in a compact Display and comfort disclosure.
- Shifted the visual treatment from digitally pristine editorial panels toward
  adult hand-painted paper and ink through fibrous texture, irregular rules,
  print-like control edges, and a painted community-square study.
- Separated the calibration progress badge from its guidance banner, stopped
  treating every missing-landmark frame as a lighting problem, and relaxed the
  MediaPipe provider confidence gate from `0.55` to the domain-aligned `0.45`.
- Corrected the real-camera left/right mismatch by normalizing MediaPipe output
  to the mirrored player preview, added ankle-first side-step recognition with
  hip fallback, averaged the stable calibration window, and exposed the single
  confidence tuning constant in `domain/movement/landmarks.ts`.
- Added live standing and seated landmark skeletons, mode-specific body/hand
  framing targets, detected-part status, and an explicit classified-movement
  readout during rehearsal so camera failures are visible instead of silent.
